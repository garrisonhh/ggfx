#version 330 core

// in screen coords (-1.0 < n < 1.0):
layout (location = 0) in vec2 dst_pos;
// in tex coords:
layout (location = 1) in vec2 src_pos;
layout (location = 2) in vec2 src_size;
// in pixels:
layout (location = 3) in vec2 src_pix;

uniform vec2 disp_size;

void main() {
    ;
}
